Put the background music track here as:

  ambient.mp3   (preferred)
  ambient.wav / ambient.ogg also work - the provider tries them in that order

Until a track exists, MusicProvider marks itself unavailable and NEITHER sound
control renders, so there are never dead buttons on the page.
